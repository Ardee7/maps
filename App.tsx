import { SafeAreaView } from "react-native";
import { NavContainer } from "./src/navigations";

function App(): React.JSX.Element {
  return (
    <SafeAreaView>
      <NavContainer />
    </SafeAreaView>
  );
}

export default App;
